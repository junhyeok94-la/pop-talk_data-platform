version: 2
models:
  - name: mart_boxoffice_daily
    columns:
      - name: boxoffice_key
        data_tests: [not_null, unique]
  - name: mart_data_quality
    columns:
      - name: exchange_ready_key
        data_tests: [not_null, unique]
      - name: movie_count_difference
        data_tests:
          - accepted_values:
              arguments: {values: [0]}
      - name: boxoffice_count_difference
        data_tests:
          - accepted_values:
              arguments: {values: [0]}
