version: 2

models:
  - name: stg_successful_exchange_publications
    columns:
      - name: publication_key
        data_tests: [not_null, unique]
  - name: stg_movie_observations
    columns:
      - name: observation_key
        data_tests: [not_null, unique]
      - name: canonical_movie_key
        data_tests: [not_null]
      - name: title_ko
        data_tests: [not_null]
      - name: record_sha256
        data_tests: [not_null]
  - name: stg_boxoffice_observations
    columns:
      - name: observation_key
        data_tests: [not_null, unique]
      - name: target_date
        data_tests: [not_null]
      - name: kofic_movie_cd
        data_tests: [not_null]
